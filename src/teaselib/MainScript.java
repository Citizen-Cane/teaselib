package teaselib;

import teaselib.functional.RunnableScript;

/**
 * @author Citizen-Cane
 *
 *         An interface to tag a script as an entry point to a project.
 */
@FunctionalInterface
public interface MainScript extends RunnableScript {
    // tag interface
}
